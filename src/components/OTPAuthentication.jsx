const OTPAuthentication = () => {
  return (
    <div>
      <p>
        Lets design a button that produces a random 4-digit number as an OTP.
      </p>
    </div>
  );
};

export default OTPAuthentication;
